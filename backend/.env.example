# Django Settings
SECRET_KEY=your-secret-key-here
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1

# Database (SQLite for development, PostgreSQL for production)
# Development uses SQLite by default - no config needed
# For PostgreSQL:
# DB_NAME=numeros
# DB_USER=numeros
# DB_PASSWORD=your-db-password
# DB_HOST=localhost
# DB_PORT=5432

# Swiss Ephemeris path (for astrology calculations)
# Download files from: https://www.astro.com/ftp/swisseph/ephe/
# EPHE_PATH=/path/to/ephe

# Email (production)
# EMAIL_HOST=smtp.example.com
# EMAIL_USER=noreply@numeros.app
# EMAIL_PASSWORD=your-email-password

# Production Only:
# DJANGO_SETTINGS_MODULE=config.settings.production
